apiVersion: v1
kind: Service
metadata:
  name: ms-api
spec:
  selector:
    app: ms-api
  ports:
    - protocol: TCP
      port: 8080

---

apiVersion: v1 
kind: Service
metadata:
  name: ms-api-gateway
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: ms-api-gateway
