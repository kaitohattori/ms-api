apiVersion: v1
kind: ConfigMap
metadata:
  name: ms-api-nginx-conf
data:
  nginx.conf: |
    server {
        listen 80;

        location / {
            proxy_pass http://127.0.0.1:8080/;
            proxy_redirect off;
        }
    }
